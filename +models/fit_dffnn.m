function model = fit_dffnn(X_train, y_train, hyperparams, seed, cfg)
% MODELS.FIT_DFFNN  Stub — fit/predict separation (Rule 10.4).
% Must return a model OBJECT, not predictions.
error('models:NotImplemented','models.fit_dffnn is not yet implemented.');
end
