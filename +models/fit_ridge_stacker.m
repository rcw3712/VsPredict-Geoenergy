function model = fit_ridge_stacker(X_train, y_train, hyperparams, seed, cfg)
% MODELS.FIT_RIDGE_STACKER  Stub — fit/predict separation (Rule 10.4).
% Must return a model OBJECT, not predictions.
error('models:NotImplemented','models.fit_ridge_stacker is not yet implemented.');
end
