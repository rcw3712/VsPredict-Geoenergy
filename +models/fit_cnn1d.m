function model = fit_cnn1d(X_train, y_train, hyperparams, seed, cfg)
% MODELS.FIT_CNN1D  Stub — fit/predict separation (Rule 10.4).
% Must return a model OBJECT, not predictions.
error('models:NotImplemented','models.fit_cnn1d is not yet implemented.');
end
