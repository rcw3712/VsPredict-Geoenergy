function model = fit_mlffnn(X_train, y_train, hyperparams, seed, cfg)
% MODELS.FIT_MLFFNN  Stub — fit/predict separation (Rule 10.4).
% Must return a model OBJECT, not predictions.
error('models:NotImplemented','models.fit_mlffnn is not yet implemented.');
end
