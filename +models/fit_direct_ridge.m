function model = fit_direct_ridge(X_train, y_train, hyperparams, seed, cfg)
% MODELS.FIT_DIRECT_RIDGE  Stub — fit/predict separation (Rule 10.4).
% Must return a model OBJECT, not predictions.
error('models:NotImplemented','models.fit_direct_ridge is not yet implemented.');
end
