function model = fit_hybrid_icnn(X_train, y_train, hyperparams, seed, cfg)
% MODELS.FIT_HYBRID_ICNN  Stub — fit/predict separation (Rule 10.4).
% Must return a model OBJECT, not predictions.
error('models:NotImplemented','models.fit_hybrid_icnn is not yet implemented.');
end
